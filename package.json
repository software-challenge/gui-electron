{
  "name": "software-challenge-gui",
  "productName": "Software-Challenge GUI",
  "version": "19.2.0",
  "description": "GUI der Software-Challenge",
  "repository": "https://github.com/CAU-Kiel-Tech-Inf/socha_graphical_client",
  "main": "main.js",
  "scripts": {
    "test": "yarn install && yarn compile",
    "start": "yarn install && yarn compile && (test \"hi\" && (test -d server || yarn getserver)) || echo I'm on Windows, so i can't test! && electron .",
    "start-dev": "yarn install && yarn compile && electron . --dev",
    "getserver": "rimraf socha && git clone git@github.com:CAU-Kiel-Tech-Inf/socha.git --depth 1 && cd socha && ./gradlew deploy -q && cd .. && rimraf server && cp -r socha/server/build/runnable server && cp socha/player/build/libs/defaultplayer.jar . && rimraf socha",
    "copyserver": "rimraf server && rimraf client && ../socha/gradlew deploy -p ../socha && cp -r ../socha/server/build/runnable server && cp ../socha/player/build/libs/defaultplayer.jar . && rimraf socha",
    "dist": "yarn install && yarn compile && electron-builder",
    "dist-all": "rimraf dist && yarn install && yarn compile && electron-builder --win --linux --mac",
    "compile": "rimraf out && tsc"
  },
  "author": "Software-Challenge Team",
  "license": "ISC",
  "build": {
    "appId": "de.software-challenge.gui",
    "files": [
      "main.js",
      "src",
      "out",
      "server"
    ],
    "asar": true,
    "asarUnpack": [
      "server"
    ],
    "directories": {
      "output": "dist",
      "buildResources": "assets/build-resources"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": "x64"
        },
        "zip"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": [
        "zip"
      ]
    },
    "linux": {
      "target": [
        "AppImage"
      ]
    }
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "deep-equal": "^1.0.1",
    "express": "^4.16.3",
    "gl-matrix": "^2.8.1",
    "hazardous": "^0.3.0",
    "phaser": "^3.13.0",
    "portfinder": "^1.0.17",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-iframe": "^1.3.0",
    "react-photonkit": "^0.4.1",
    "request": "^2.88.0",
    "sax": "^1.2.4",
    "tree-kill": "^1.2.0",
    "xml2js": "^0.4.19",
    "validate-typescript": "^2.0.1"
  },
  "devDependencies": {
    "@types/body-parser": "^1.16.8",
    "@types/es6-promise": "^0.0.32",
    "@types/express": "^4.11.0",
    "@types/react": "^16.0.36",
    "@types/react-dom": "^16.0.3",
    "@types/request": "^2.0.13",
    "@types/sax": "^1.0.0",
    "electron": "^3.0.0",
    "electron-builder": "^20.28.4",
    "electron-builder-squirrel-windows": "^20.28.3",
    "rimraf": "^2.6.2",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  }
}
